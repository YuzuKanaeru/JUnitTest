package org.example

//Membuat kelas kalkulator
class Calculator {
    //Membuat method dua data dan return dari sum data tersebut
    fun add(first: Int, second: Int): Int{
        return first + second
    }

    //Membuat method sub baru
    fun sub(first: Int, second: Int): Int{
        return  first - second
    }
}